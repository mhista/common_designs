hey pal, so i want to build out the mobile app portion for vendors. now, I noticed a short coming in our architect. and that's in terms of chatting with the AI and genUI. now, we created schema for the AI communicating with the backend and toolings. this was in tandem with whatsapp and telegram way of communicating which works well. but now that we are building the app, it communictes directly with the server. and there's no provision for that. and we need to build that in. an endpoint for  communicates incoming messages from phone, and also, this should allow bypassing certain constraints. and a custom schema should be built for both the serverside and flutter. this schema will allow bi derectional communication, and will support streaming, so that messages will be showing as the results of the request is being processed, and this schema should support ui too, the idea is like the way ai chats work. when talking with them, the should know when a text annswer should be sent alone(markdown will be used to show it), and when it requires both text and widget and  just widget. an example is when the vendor asks for the list of current selling products, the schema and ai should be able to deduce that this might require a list of product widgets and should be able to send this details to. the aim is to fix the shortcomings of whatsapp, and telegram and to also give the best user experience. remember the goal of modularity, composability and reusability. now the schema should be in a way that we can just unplug it from our agentsystem and use a custom ai directly by giving it a system instruction and fuction tools, and should also be able to use our agent system and the existing function tools in place. so update places that needs upadating and create stuffs that needs creating. this means that he flutter part should not be heavily dependant on the serverpod. let's i opt to use firebase and maybe firebase ai logic. will the genUi on the flutter side work? thats the essence of modularity and composability, i am talking about. decoupling ability. also, to add to it, modify the auth endpoint, and associated auth services, and classes this way to accomodate mobile. when a user, there shoud be a way to track that a user is comin from mobile, or web and not from whatsapp or telegram. now the key signin or signup here will be the mobile number. and if detected and the user is not already registered, a message will be sent using that number to the users whatsapp, to complete the normal registration process, but if the user has already registered on whatsapp, before opening the app, when the user tries to signin, a code will be sent to whatsapp, and user uses it to verify. since a user doesn't need to always sign in everytime, you sjould know what to do about that right?. something like a token that will be used to verify.